apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "resume-web.fullname" . }}-test-connection"
  labels:
    {{- include "resume-web.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox:1.36
      command: ['wget']
      args: ['{{ include "resume-web.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
